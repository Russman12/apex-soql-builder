{
  "name": "salesforce-app",
  "private": true,
  "version": "1.0.0",
  "description": "Salesforce App",
  "scripts": {
    "lint": "npm run lint:apex && npm run lint:doc",
    "lint:apex": "sf code-analyzer run --target 'force-app/**/*.cls' --rule-selector 'pmd:apex-soql-builder'",
    "lint:doc": "npm run write-good README.md --parse",
    "test": "sf apex run test --result-format human --output-dir ./tests/apex --test-level RunLocalTests --wait 20",
    "test:coverage": "sf apex run test --result-format human --output-dir ./tests/apex --test-level RunLocalTests --code-coverage --detailed-coverage --wait 20",
    "prettier": "prettier --write \"**/*.{cls,cmp,component,css,html,js,json,md,page,trigger,xml,yaml,yml}\" --cache",
    "prettier:verify": "prettier --check \"**/*.{cls,cmp,component,css,html,js,json,md,page,trigger,xml,yaml,yml}\"",
    "precommit": "npm run prettier && npm run lint",
    "version:create": "sf package version create --package \"Apex SOQL Builder\" --installation-key-bypass --wait 10",
    "data:reset": "sf apex run --file ./data/reset.apex",
    "data:seed": "sf data import tree --plan data/plan.json",
    "scratch": "sf org create scratch --definition-file config/project-scratch-def.json --alias apexsoqlbuilder --duration-days 7 --set-default --json",
    "create": "npm run scratch && sf project deploy start && npm run data:seed",
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.2.2",
    "@commitlint/config-conventional": "^19.8.1",
    "@prettier/plugin-xml": "^3.2.2",
    "husky": "^9.1.7",
    "prettier": "^3.1.0",
    "prettier-plugin-apex": "^2.0.1",
    "semantic-release": "^24.2.8",
    "write-good": "^1.0.8"
  },
  "dependencies": {
    "@semantic-release/exec": "^7.1.0",
    "@semantic-release/release-notes-generator": "^14.1.0"
  }
}
